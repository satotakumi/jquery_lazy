{
  "name": "jquery_lazy",
  "version": "0.0.1",
  "description": "jQuery plugin for lazy doing something",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "start": "gulp serve",
    "build": "npm run clean && gulp build",
    "watch": "gulp watch",
    "deploy": "npm run build && gulp minify-html && gulp deploy && rimraf .tmp",
    "preinstall": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sotayamashita/jquery_lazy.git"
  },
  "author": "Sota Yamashita",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sotayamashita/jquery_lazy/issues"
  },
  "homepage": "https://github.com/sotayamashita/jquery_lazy#readme",
  "devDependencies": {
    "browser-sync": "^2.14.0",
    "gulp": "^3.9.1",
    "gulp-gh-pages": "^0.5.4",
    "gulp-header": "^1.8.7",
    "gulp-htmlmin": "^2.0.0",
    "gulp-load-plugins": "^1.2.4",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^2.0.0",
    "rimraf": "^2.5.4"
  }
}
